# pages

一頁式公開內容 hub。源檔在 `index.html`，網址:

- https://taiwanbestphoto.github.io/pages/

## 本地預覽

```bash
open index.html        # macOS 直接看
python3 -m http.server 8000   # 或起 local server
```

## 怎麼更新內容

直接編輯 `index.html`，commit 後 push `main` 分支；GitHub Pages
會在約 30 秒內自動重新部署。
